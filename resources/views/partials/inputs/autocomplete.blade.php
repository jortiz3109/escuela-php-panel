<i-autocomplete
    label="{{ $field->label }}"
    id="{{ $field->name }}"
    name="{{ $field->name }}"
    placeholder="{{ $field->placeholder }}"
    :data='{{ $field->data }}'>
</i-autocomplete>
